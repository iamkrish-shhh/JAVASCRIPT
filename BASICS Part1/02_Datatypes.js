"use strict";   //treat all JS code as newer version

// alert(3 + 4);   //we are using node js , not browser, so alert is not defined, we will use console.log instead

console.log(3 + 4);   

let name = "krish"
let age = 21
let isAdult = true

    // data types in JS
// number => 2 to power 53     //in JS ,Float and integer => number
// bigint
// string = "krish" 'krish' `krish`
// boolean => true/false
// null => standalone value
// undefined
// symbol => unique



// object
console.log(typeof undefined); // undefined
console.log(typeof null); // object
